<!DOCTYPE html>
<html lang="en">
<head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title><%= title %></title>
</head>
<body>
     <%- include('partials/nav.ejs'); %>

     <% if(!locals.games) { %>
          <span>No games were found, this shouldnt happen</span>
     <% } else %>
     <% games.forEach(game => { %>
          <%- include('partials/game.ejs', {game: game}); %>
     <% }) %>
</body>
</html>
